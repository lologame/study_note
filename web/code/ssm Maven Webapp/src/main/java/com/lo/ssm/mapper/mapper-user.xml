<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.lo.ssm.mapper.UserMapper" >
	<select id="getUser4Login" resultType="com.lo.ssm.beans.User" parameterType="com.lo.ssm.beans.User">
		select * from tbl_user where username = #{username} and password = #{password}
	</select>
</mapper>